# Калькулятор трат и мечты

Учебный проект: простой веб-калькулятор для контроля расходов и расчёта срока накопления на цель.

## Что умеет приложение

- Указать ежемесячный доход
- Добавить категории трат с периодичностью (в день / неделю / месяц)
- Задать цель (например, «Поездка в Японию — 200 000 ₽»)
- Увидеть: сколько уходит, сколько остаётся, через сколько месяцев накопите
- Данные сохраняются в браузере — не пропадают после закрытия вкладки

## Структура файлов

```
firstProject/
├── index.html    — разметка страницы
├── style.css     — стили
├── app.js        — вся логика (расчёты, localStorage, обновление UI)
├── app.test.js   — тесты чистых функций
└── README.md     — этот файл
```

## Как запустить

1. Скачайте или клонируйте папку проекта на свой компьютер
2. Откройте файл `index.html` двойным кликом — он откроется в браузере
3. Готово. Никакого Node.js, серверов и установки зависимостей не нужно

## Как запустить тесты

Тесты запускаются автоматически при каждом открытии страницы.

1. Откройте `index.html` в браузере
2. Нажмите `F12` → вкладка **Console**
3. Вы увидите группу `🧪 Тесты app.js` — каждый тест помечен `✅` (прошёл) или `❌` (упал)

## Как расширить проект (идеи для V2)

- Добавить графики расходов по категориям
- История трат по датам
- Несколько целей одновременно
- Экспорт/импорт данных в JSON-файл
- Тёмная тема
